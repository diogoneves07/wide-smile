/**
 * Gets a random key.
 * @param keys
 * The array of numeric values.
 *
 * @param lastkey
 * The array of numeric values.
 */
export default function getRandomKey(keys: number[], lastkey?: number): number;
