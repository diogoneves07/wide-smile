/**
 * Force the animation returns their progress to a certain point and end the interaction.
 */
export default function progressAnimationGoTo(animationId: number, newProgress: number, applyDelay?: boolean): void;
